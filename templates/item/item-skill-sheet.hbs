<form class="{{cssClass}} flexcol" autocomplete="off">
  <header class="sheet-header">
    <img class="profile-img" src="{{item.img}}" data-edit="img" title="{{item.name}}"/>
    <div class="header-fields">
      <h1 class="charname">
        <input name="name" type="text" value="{{item.name}}" placeholder="Skill name"/>
      </h1>
      <div class="grid grid-2col">
        <div class="resource">
          <label class="resource-label">{{localize "ZOMBICIDE.Skill.UnlockedAt"}}</label>
          <select name="system.dangerLevel" class="danger-{{system.dangerLevel}}">
            {{selectOptions config.dangerLevels selected=system.dangerLevel localize=true}}
          </select>
        </div>
        <div class="resource">
          <label class="resource-label">{{localize "ZOMBICIDE.Skill.ActionTypeLabel"}}</label>
          <select name="system.actionType">
            {{selectOptions config.skillActionTypes selected=system.actionType localize=true}}
          </select>
        </div>
      </div>
    </div>
  </header>

  {{!-- Danger level and action type live in the header: repeating them in a
        tab would submit two values for the same field. --}}
  <nav class="sheet-tabs tabs" data-group="primary">
    <a class="item" data-tab="description">Description</a>
    <a class="item" data-tab="effects">Effects</a>
  </nav>

  <section class="sheet-body">

    <div class="tab" data-group="primary" data-tab="description">
      {{editor enrichedDescription target="system.description" engine="prosemirror" button=true editable=editable}}
    </div>

    <div class="tab effects" data-group="primary" data-tab="effects">
      {{> "systems/zombicide-chronicles/templates/item/parts/item-effects.hbs"}}
    </div>

  </section>
</form>
